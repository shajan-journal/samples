[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "text-chunking"
version = "0.1.0"
description = "Sample corpus/index playground"
requires-python = ">=3.10"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["text_chunking"]
